import { combineReducers } from 'redux';

// Импортируйте свои редьюсеры здесь
// import userReducer from './userReducer';

const rootReducer = combineReducers({
  // user: userReducer,
});

export default rootReducer;
